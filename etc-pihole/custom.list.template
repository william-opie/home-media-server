${LAN_IP} jellyfin.${DOMAIN}
${TAILSCALE_IP} jellyfin.${DOMAIN}
${LAN_IP} pihole.${DOMAIN}
${TAILSCALE_IP} pihole.${DOMAIN}
${LAN_IP} home.${DOMAIN}
${TAILSCALE_IP} home.${DOMAIN}
${LAN_IP} qbittorrent.${DOMAIN}
${TAILSCALE_IP} qbittorrent.${DOMAIN}
${LAN_IP} bazarr.${DOMAIN}
${TAILSCALE_IP} bazarr.${DOMAIN}
${LAN_IP} flaresolverr.${DOMAIN}
${TAILSCALE_IP} flaresolverr.${DOMAIN}
${LAN_IP} jellyseerr.${DOMAIN}
${TAILSCALE_IP} jellyseerr.${DOMAIN}
${LAN_IP} lidarr.${DOMAIN}
${TAILSCALE_IP} lidarr.${DOMAIN}
${LAN_IP} prowlarr.${DOMAIN}
${TAILSCALE_IP} prowlarr.${DOMAIN}
${LAN_IP} radarr.${DOMAIN}
${TAILSCALE_IP} radarr.${DOMAIN}
${LAN_IP} sonarr.${DOMAIN}
${TAILSCALE_IP} sonarr.${DOMAIN}